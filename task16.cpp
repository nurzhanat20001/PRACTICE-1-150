#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	int a,b,c;
	cin >> a>>b>>c;

	cout << a*2 << endl << b/3.0<<endl<<pow(c,2)<<endl<<( a * 2)+( b / 3.0)+ (pow(c, 2));
	return 0;
}